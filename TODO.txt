 * add Sphinx docs
 * add buildout.cfg that builds development env for collective.table
 * add jenkins.cfg so we can add this project to jenkins.plone.org
 * add plone.app.testing tests
 * add support for Selenium tests
